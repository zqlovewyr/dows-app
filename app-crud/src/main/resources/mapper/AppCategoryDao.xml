<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dows.app.dao.AppCategoryDao">

    <resultMap type="org.dows.app.entity.AppCategory" id="AppCategoryMap">
        <result property="id" column="id" jdbcType=""/>
        <result property="pid" column="pid" jdbcType=""/>
        <result property="categName" column="categ_name" jdbcType=""/>
        <result property="categCode" column="categ_code" jdbcType=""/>
        <result property="descr" column="descr" jdbcType=""/>
        <result property="deleted" column="deleted" jdbcType=""/>
        <result property="categId" column="categ_id" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="AppCategoryMap">
        select id,
               pid,
               categ_name,
               categ_code,
               descr,
               deleted,
               categ_id
        from app_category
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="AppCategoryMap">
        select
        id, pid, categ_name, categ_code, descr, deleted, categ_id
        from app_category
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="pid != null">
                and pid = #{pid}
            </if>
            <if test="categName != null and categName != ''">
                and categ_name = #{categName}
            </if>
            <if test="categCode != null and categCode != ''">
                and categ_code = #{categCode}
            </if>
            <if test="descr != null and descr != ''">
                and descr = #{descr}
            </if>
            <if test="deleted != null">
                and deleted = #{deleted}
            </if>
            <if test="categId != null and categId != ''">
                and categ_id = #{categId}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from app_category
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="pid != null">
                and pid = #{pid}
            </if>
            <if test="categName != null and categName != ''">
                and categ_name = #{categName}
            </if>
            <if test="categCode != null and categCode != ''">
                and categ_code = #{categCode}
            </if>
            <if test="descr != null and descr != ''">
                and descr = #{descr}
            </if>
            <if test="deleted != null">
                and deleted = #{deleted}
            </if>
            <if test="categId != null and categId != ''">
                and categ_id = #{categId}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into app_category(pid, categ_name, categ_code, descr, deleted, categ_id)
        values (#{pid}, #{categName}, #{categCode}, #{descr}, #{deleted}, #{categId})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into app_category(pid, categ_name, categ_code, descr, deleted, categ_id)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.pid}, #{entity.categName}, #{entity.categCode}, #{entity.descr}, #{entity.deleted},
            #{entity.categId})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into app_category(pid, categ_name, categ_code, descr, deleted, categ_id)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.pid}, #{entity.categName}, #{entity.categCode}, #{entity.descr}, #{entity.deleted},
            #{entity.categId})
        </foreach>
        on duplicate key update
        pid = values(pid),
        categ_name = values(categ_name),
        categ_code = values(categ_code),
        descr = values(descr),
        deleted = values(deleted),
        categ_id = values(categ_id)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update app_category
        <set>
            <if test="pid != null">
                pid = #{pid},
            </if>
            <if test="categName != null and categName != ''">
                categ_name = #{categName},
            </if>
            <if test="categCode != null and categCode != ''">
                categ_code = #{categCode},
            </if>
            <if test="descr != null and descr != ''">
                descr = #{descr},
            </if>
            <if test="deleted != null">
                deleted = #{deleted},
            </if>
            <if test="categId != null and categId != ''">
                categ_id = #{categId},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from app_category
        where id = #{id}
    </delete>

</mapper>

